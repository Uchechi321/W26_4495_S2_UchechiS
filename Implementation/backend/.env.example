# Copy to .env in this folder (backend/) and fill in. Never commit .env.

# Optional: used for LLM segment analysis features
OPENAI_API_KEY=

# Optional (default: gpt-4o-mini)
# SEGMENT_ANALYSIS_MODEL=gpt-4o-mini
